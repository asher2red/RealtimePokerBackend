version: '3.9'

services:

  backend:
    build: .
    ports:
      - "5114:8080"

    depends_on:
      - redis

    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - RedisConnection=redis:6379

  redis:
    image: redis:7-alpine

    ports:
      - "6379:6379"